import { englishServiceLinks } from "../lib/seo";
import { pageCopy } from "../i18n/page-copy";
import type { Locale } from "../i18n/translations";
import LanguageLinks from "./LanguageLinks";
import Link from "next/link";
import { EMAIL, marketLinks, serviceLinks, whatsappUrl } from "../lib/site";
export default function SiteFooter({locale = "fr"}: {locale?: Locale}){const copy = pageCopy[locale]; return <footer className="border-t border-white/10 bg-[#050505]"><div className="container grid gap-10 py-14 md:grid-cols-4"><div><p className="text-xl font-black">Soufiane Boutatss</p><p className="mt-3 text-sm leading-6 text-white/55">{copy.description}</p></div><div><h2 className="text-sm font-bold uppercase tracking-wider">{copy.serviceTitle}</h2><ul className="mt-4 space-y-2">{(locale === "en" ? englishServiceLinks : serviceLinks).map((l,i)=><li key={l.href}><Link className="text-sm text-white/55 hover:text-[#ff6b00]" href={l.href}>{locale === "en" ? l.label : copy.services[i]}</Link></li>)}</ul></div><div><h2 className="text-sm font-bold uppercase tracking-wider">{copy.zones}</h2><ul className="mt-4 space-y-2">{marketLinks.map((l,i)=><li key={l.href}><Link className="text-sm text-white/55 hover:text-[#ff6b00]" href={l.href}>{copy.markets[i]}</Link></li>)}</ul></div><div><h2 className="text-sm font-bold uppercase tracking-wider">{copy.contact}</h2><p className="mt-4 text-sm text-white/55">{copy.remote}</p><a className="mt-2 block text-sm text-white/70 hover:text-[#ff6b00]" href={`mailto:${EMAIL}`}>{EMAIL}</a><a className="mt-4 inline-block rounded-full border border-[#ff6b00]/50 px-4 py-2 text-sm font-bold text-[#ff8a3d]" href={whatsappUrl("Bonjour Soufiane, je souhaite discuter de mon projet web.")} target="_blank" rel="noopener noreferrer">WhatsApp</a></div></div><div className="border-t border-white/10 py-5"><div className="container flex flex-wrap justify-between gap-4 text-xs text-white/35"><span>© {new Date().getFullYear()} Soufiane Boutatss.</span><nav className="flex gap-4"><Link href="/mentions-legales">{copy.legal}</Link><Link href="/politique-confidentialite">{copy.privacy}</Link></nav></div></div><div className="container pb-6"><LanguageLinks locale={locale}/></div></footer>}
