# --- Visitor analytics / admin dashboard ---

# Neon Postgres connection string.
# Auto-added when you connect a Neon database in Vercel → Storage tab.
# For local dev, copy it from the Neon/Vercel dashboard into .env.local.
DATABASE_URL=

# Password that gates the /admin dashboard. Pick something strong.
ADMIN_PASSWORD=change-me
